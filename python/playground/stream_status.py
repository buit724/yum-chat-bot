    # TODO remove this, only here for testing checking if a stream is active
    #streams: List[Stream] = [x async for x in twitch.get_streams(user_id=[users[2].id])]
    #stream = streams[0]
    #print(f"type: {stream.type}")
    #print(f"title: {stream.title}")
    #print(f"category: {stream.game_name}")
    #print(f"viewer count: {stream.viewer_count}")
    #print(f"tags: {stream.tags}")
    #print (stream.gam)

